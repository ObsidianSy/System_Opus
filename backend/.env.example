# ============================================
# BACKEND - Configurações do Servidor e Banco
# ============================================
# 
# INSTRUÇÕES:
# 1. Copie este arquivo para: backend/.env
# 2. Preencha as variáveis com seus valores reais
# 3. NUNCA commite o arquivo .env no Git!

# 🔐 SEGURANÇA
# Secret key para JWT - Gere um valor único e seguro
# Para gerar um novo secret, execute: 
#   node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=SUA_CHAVE_SECRETA_AQUI_64_CARACTERES_MINIMO

# Configurações do PostgreSQL
DB_HOST=docker_opus_one
DB_PORT=5432
DB_NAME=docker
DB_USER=postgres
DB_PASSWORD=bb6cc576ca06d83f4b3d

# Porta do servidor backend
PORT=3001

# Ambiente (development ou production)
NODE_ENV=development

# URLs permitidas pelo CORS (separadas por vírgula)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# ============================================
# FRONTEND - Não precisa de .env
# ============================================
# O frontend já está configurado para usar:
# - http://localhost:3001/api em desenvolvimento
# - Você pode alterar em: src/services/n8nIntegration.ts
